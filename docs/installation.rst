============
Installation
============

At the command line::

    $ easy_install service_discovery

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv service_discovery
    $ pip install service_discovery
